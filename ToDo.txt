used the cookie but not actually implemented it for that... it need to be done in the login, registration and dashboard page and manually remove the jwt string holder because axios already do that... we are doing the http only cookie to prevent the token storing in the localstorage and the user session can be hijacked. This is to provide security....    